# Local SQLite mode: leave DATABASE_URL empty.
# Cloud Postgres mode: paste Supabase / Neon pooled connection string.
DATABASE_URL=

# Required after deployment. Keep it secret.
ADMIN_PASSWORD=change-this-password

# Frontend domain(s), comma-separated.
CORS_ORIGINS=http://localhost:3000,https://your-vercel-app.vercel.app
