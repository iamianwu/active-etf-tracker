name: Update Yahoo All Quotes Slow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Disabled. Use 'Update TWSE MIS Quotes' instead."
