name: Update Yahoo All Quotes

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Disabled because cloud IPs were blocked or rate-limited."
          echo "Use Update Official Close Quotes instead."
